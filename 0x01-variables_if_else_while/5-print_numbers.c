#include <stdio.h>
/**
 * main - this is where the code starts execution
 *
 * Return: This code should return o after execution
 */
int main(void)
{
	int n;

	for (n == 0; n < 10; n++)
	{
		printf("%d\n", n);
	}
	return (0);
}
