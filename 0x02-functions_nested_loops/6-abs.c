#include "main.h"
/**
 * _abs - this is the ABS function
 * @n: this has only int as an attribute
 *
 * Return: this shoulkd return 0 after compillilation
 * Write a function that computes the absolute value of an integer
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
