#include "main.h"
/**
 * _isalpha - this is the main function i am calling
 * @c: this is the int c function too
 *
 * Return: this function is supposed to return 1
 */
int _isalpha(int c)
{
	return ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'));
}
